const token=sessionStorage.getItem('ngoToken');if(!token)location='login.html';const h={'Content-Type':'application/json','X-NGO-Token':token};const setDates=()=>document.querySelectorAll('input[type=date]').forEach(i=>i.value=new Date().toISOString().slice(0,10));async function load(){const c=await fetch('/api/campaigns').then(r=>r.json());document.querySelectorAll('.campaign-select').forEach(s=>s.innerHTML=c.map(x=>`<option value="${x.id}">${x.name}</option>`).join(''));}document.querySelectorAll('.tab').forEach(t=>t.onclick=()=>{document.querySelectorAll('.tab,.entry-form').forEach(x=>x.classList.remove('active'));t.classList.add('active');document.getElementById(t.dataset.form).classList.add('active');});document.querySelectorAll('.entry-form').forEach(f=>f.onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(f));['goal','amount','campaignId'].forEach(k=>{if(d[k])d[k]=Number(d[k])});if(!d.donorName)d.donorName='Anonymous';const r=await fetch('/api/'+f.id.replace('Form','s'),{method:'POST',headers:h,body:JSON.stringify(d)});if(r.status===401){sessionStorage.clear();location='login.html';return;}document.getElementById('message').textContent=r.ok?'Saved! Donors can now see this update.':'Could not save.';if(r.ok){f.reset();setDates();load();}});document.getElementById('logout').onclick=()=>{sessionStorage.clear();location='login.html';};setDates();load();
