document.getElementById('loginForm').addEventListener('submit',async e=>{e.preventDefault();const r=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});if(!r.ok){document.getElementById('loginMessage').textContent='Incorrect email or password.';return;}const d=await r.json();sessionStorage.setItem('ngoToken',d.token);window.location='ngo-dashboard.html';});
